#!/bin/bash

if [ "$1" = refine_arch ]; then exit 1; fi # no-op for now
true

echo "Setting-up ~/usr/debian/..."


#cd
#for f in devrc ; do
#    link_to usr/arch/etc/"$f" ~/".$f" "$LOCAL_CONFIG_BACKUP"
#done
